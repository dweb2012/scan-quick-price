const TopBar = () => (
  <header className="bg-topbar text-topbar-foreground px-4 py-3 flex items-center justify-center shadow-md z-50 safe-area-top">
    <h1 className="text-lg font-bold tracking-tight">CHR PriceScanner</h1>
  </header>
);

export default TopBar;
